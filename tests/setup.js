/**
 * Setup global para testes Vitest.
 *
 * - Importa matchers extras do @testing-library/jest-dom para testes de componentes
 *   (ex: toBeInTheDocument, toHaveTextContent)
 * - Pode ser estendido com mocks globais no futuro
 */
import '@testing-library/jest-dom/vitest';
